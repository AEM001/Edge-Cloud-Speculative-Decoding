# PicoSpec 实验平台介绍

这是一个用于研究 **Speculative Decoding** 技术的实验平台，支持同步和异步两种解码模式，用于验证草稿模型加速大模型推理的效果。

---

## 整体架构

平台采用 **边缘-云端** 架构：

- **边缘端（GPU 1）**：运行 1.5B 参数的小模型（草稿模型），快速生成草稿 token
- **云端（GPU 0）**：运行 7B 参数的大模型（验证模型），验证并修正草稿

两者通过 HTTP 协议通信，模拟真实的网络延迟环境。

```
┌─────────────────┐     HTTP      ┌─────────────────┐
│   边缘端 GPU1   │  <────────>  │   云端 GPU0     │
│  Qwen 1.5B AWQ  │   延迟模拟    │  Qwen 7B AWQ   │
│  生成草稿token  │               │  验证并修正    │
└─────────────────┘               └─────────────────┘
```

---

## 功能组件

### 核心模块

| 文件 | 功能 |
|------|------|
| `config.py` | 全局配置，支持环境变量覆盖 |
| `protocol.py` | 通信协议数据结构（请求/响应） |
| `model_manager.py` | 草稿模型加载（vLLM） |
| `draft_generator.py` | 生成 K 个草稿 token |

### 客户端

| 文件 | 功能 |
|------|------|
| `edge_client.py` | **同步推测解码**：草稿→等待验证→下一批 |
| `async_edge_client.py` | **异步推测解码**：草稿和验证并行（Pipeline） |
| `http_cloud_client.py` | HTTP 客户端，连接验证服务器 |

### 服务端

| 文件 | 功能 |
|------|------|
| `verify_server.py` | FastAPI 服务，加载 7B 模型，提供验证接口 |

### 辅助模块

| 文件 | 功能 |
|------|------|
| `policies/policies.py` | K 值选择策略（固定 K=2/4/8） |
| `benchmarks/network_simulator.py` | 网络延迟和带宽模拟 |
| `benchmarks/prompts.txt` | 测试用的简单/复杂提示词 |

---

## 整体逻辑

### 同步推测解码流程

```
1. 边缘端用 1.5B 模型生成 K 个草稿 token
2. 发送给云端验证
3. 云端用 7B 模型验证每个 token（贪心策略：argmax 匹配则接受）
4. 返回接受长度 + 修正 token（如有拒绝）
5. 边缘端更新前缀，重复步骤 1-4 直到生成长度达标
```

**特点**：简单直接，但每次必须等待云端返回才能继续下一批。

### 异步推测解码流程（PicoSpec）

```
1. 边缘端持续生成草稿，不等待验证结果
2. 后台线程将草稿提交给云端验证
3. 验证结果通过队列返回
4. 遇到全命中（full hit）：前缀前进，继续生成
5. 遇到拒绝：回滚到已验证位置，清空后续草稿
```

**特点**：利用验证时间重叠生成时间，降低延迟。

---

## 关键指标

### 基础指标

| 指标 | 说明 |
|------|------|
| **接受率** | 草稿 token 被云端接受的比例 |
| **生成速度** | tokens/second |
| **端到端延迟** | 总耗时（毫秒） |
| **轮次** | 完成生成需要的验证轮数 |

### 异步特有指标

| 指标 | 说明 |
|------|------|
| **Pipeline 效率** | 全命中轮次占比 |
| **气泡时间** | 生成器等待验证结果的平均时间 |
| **预取浪费率** | 因回滚丢弃的草稿 token 比例 |
| **加速比** | 异步 vs 同步的速度提升 |

---

## 网络模拟

平台支持模拟四种网络环境：

| 场景 | RTT（往返延迟） | 带宽 | 适用场景 |
|------|----------------|------|----------|
| **LAN** | 40ms | 100 Mbps | 数据中心内网 |
| **WAN** | 100ms | 20 Mbps | 跨区域网络 |
| **Edge** | 200ms | 5 Mbps | 边缘设备（4G/5G） |
| **Bursty** | 40~240ms 动态 | 动态 | 网络抖动场景 |

### 模拟方式

```python
# 发送请求前：sleep(单向延迟)
# 接收响应后：sleep(单向延迟 + 传输延迟)
```

通过 `time.sleep()` 模拟延迟，通过计算 payload 大小模拟带宽限制。

---

## 实验设计

### Run3 实验（当前主要实验）

**对比三种方法**：
1. **Direct**：直接用 7B 模型生成（基线）
2. **Sync K=4/8/12**：同步推测解码，固定 K
3. **Async K=4/8/12**：异步推测解码，固定 K

**网络条件**：分别测试 LAN / WAN / Edge 三种延迟

**测试数据**：5 个简单提示词 + 5 个复杂提示词

**输出**：
- 每种组合的生成速度、接受率、延迟
- 对比图表（speedup、acceptance、latency breakdown）

### 快速测试

```bash
python quick_test.py
```

对比 Direct vs K=2 vs K=4，使用 4 个提示词快速验证系统是否正常。

---

## 使用方法

### 1. 启动验证服务器（GPU 0）

```bash
bash scripts/start_verify.sh
```

### 2. 运行快速测试（GPU 1）

```bash
CUDA_VISIBLE_DEVICES=1 python quick_test.py
```

### 3. 运行完整实验（GPU 1）

```bash
bash scripts/run_experiment.sh
```

结果保存在 `experiments/outputs_run3_<日期>/`

---

## 模型配置

- **草稿模型**：Qwen2.5-1.5B-Instruct-AWQ（4-bit 量化）
- **验证模型**：Qwen2.5-7B-Instruct-AWQ（4-bit 量化）
- **模型位置**：`/root/code/` 目录下

可通过环境变量修改路径：

```bash
DRAFT_MODEL_PATH=/path/to/draft VERIFY_MODEL_PATH=/path/to/verify python quick_test.py
```

---

## 关键发现

- **同步推测解码**：在网络延迟 > 草稿生成时间时有效
- **异步推测解码**：在网络不稳定或延迟较高时收益更大
- **K 值选择**：过小浪费验证能力，过大浪费草稿 effort，存在最优值
- **接受率**：与提示词难度相关，简单任务接受率高

---

## 文件结构

```
draft/
├── config.py              # 配置
├── protocol.py            # 数据结构
├── model_manager.py       # 模型加载
├── draft_generator.py     # 草稿生成
├── client/
│   ├── edge_client.py     # 同步客户端
│   ├── async_edge_client.py  # 异步客户端
│   └── http_cloud_client.py    # HTTP 客户端
├── server/
│   └── verify_server.py   # 验证服务器
├── benchmarks/
│   ├── network_simulator.py  # 网络模拟
│   └── prompts.txt        # 测试数据
├── experiments/
│   └── experiment_async_pipeline_run3.py  # 主实验
└── scripts/
    ├── start_verify.sh    # 启动服务脚本
    └── run_experiment.sh  # 运行实验脚本
```
